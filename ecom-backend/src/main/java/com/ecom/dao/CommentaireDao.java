/*package com.ecom.dao;

import com.ecom.entity.Commentaire;
import com.ecom.entity.Product;
import com.ecom.entity.User;
import org.springframework.data.repository.CrudRepository;
import org.springframework.security.core.parameters.P;

import java.util.List;

public interface CommentaireDao extends CrudRepository<Commentaire, Integer> {

    public List<Commentaire> findByUser(User user);

    public List<Commentaire> findByProduct(Product formation);
}
*/
